<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
     <!-- dialog样式 -->
    <style name="MyDialogStyle">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    
    <!-- dialog属性 -->
    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/trans</item>
        <!-- <item name="android:backgroundDimEnabled">false</item> -->
    </style>
    
    <style name="animation">
        <item name="android:windowEnterAnimation">@anim/translats_status_in</item>
        <item name="android:windowExitAnimation">@anim/translats_status_out</item>
    </style>
    
    <style name="animation_out">
        <item name="android:windowEnterAnimation">@anim/translats_staus_center_in</item>
        <item name="android:windowExitAnimation">@anim/translats_staus_center_out</item>
    </style>
    
    <style name="button_text">
        <item name="android:textColor">#555555</item>
        <item name="android:textSize">12sp</item>
    </style>
    
    <style name="filt_dialog" parent="@android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item> 
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        
    </style>
    
    <!-- 个人设置字体属性 -->
    <style name="style_personal_setting">
        <item name="android:textSize">@dimen/personal_setting_text</item>
        <item name="android:textColor">#000000</item>
    </style>
    
    <style name="status_list_item">
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:textColor">#373737</item>
        <item name="android:textSize">14sp</item>
    </style>
    <style name="group_label">
        <item name="android:gravity">left|center_vertical</item>
        <item name="android:padding">8dp</item>
        <item name="android:textColor">#333</item>
        <item name="android:textSize">14sp</item>
        <item name="android:background">@drawable/group_label_backgroud_selector</item>
    </style>

</resources>